To be able to work on this project need to install modules listed in install.txt file
pip install -r install.txt 

Secret key is hidden outside the repo in json file.
Path to the file -> settings.py.
I will send the file to anyone interested on working together.
